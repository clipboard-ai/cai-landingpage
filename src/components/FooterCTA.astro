---
---

<section class="footer-cta">
  <div class="container">
    <h2 class="cta-title">Get Cai:</h2>
    
    <a href="https://github.com/soyasis/cai/releases/download/v1.0.0/Cai-1.0.0-macos.dmg" class="cta-button" target="_blank" rel="noopener noreferrer">
      Download for Mac
    </a>
  </div>
</section>

<style>
  .footer-cta {
    background: linear-gradient(135deg, var(--coral-400) 0%, var(--coral-600) 50%, var(--coral-700) 100%);
    background-size: 200% 200%;
    animation: gradient-shift 8s ease infinite;
    padding: 6rem 0;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .footer-cta::before {
    content: '';
    position: absolute;
    top: -30%;
    left: -10%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.15) 0%, transparent 70%);
    border-radius: 50%;
    animation: float-up 7s ease-in-out infinite alternate;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    position: relative;
    z-index: 1;
  }

  .cta-title {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    color: white;
    margin-bottom: 2rem;
    font-weight: 700;
    text-shadow: 0 2px 20px rgba(0, 0, 0, 0.15);
  }

  .cta-button {
    display: inline-block;
    background: white;
    color: var(--grey);
    padding: 1.25rem 3rem;
    border-radius: 16px;
    font-weight: 600;
    font-size: 1.15rem;
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
    box-shadow: 0 10px 35px rgba(0, 0, 0, 0.25), 0 4px 12px rgba(0, 0, 0, 0.15);
  }

  .cta-button:hover {
    transform: translateY(-5px) scale(1.03);
    box-shadow: 0 15px 45px rgba(0, 0, 0, 0.3), 0 6px 16px rgba(0, 0, 0, 0.2);
  }

  @media (max-width: 768px) {
    .footer-cta {
      padding: 4.5rem 0;
    }

    .cta-title {
      margin-bottom: 2rem;
      font-size: 2rem;
    }

    .cta-button {
      padding: 1.1rem 2.5rem;
      font-size: 1.05rem;
    }
  }
</style>
